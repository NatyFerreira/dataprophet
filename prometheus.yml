global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'dataprophet'
    static_configs:
      # Dev mode (local): prometheus runs directly on host
      # Docker mode: change to host.docker.internal:8000
      - targets: ['localhost:8000']